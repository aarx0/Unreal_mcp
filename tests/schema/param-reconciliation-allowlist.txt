# Exemptions for tests/schema/param-reconciliation-test.ps1 (one name per line, # comments).
# Regenerate: pwsh -File tests/schema/param-reconciliation-test.ps1 -UpdateAllowlist
# Review the diff before committing: a new entry is either an internal/indirectly-read
# key or a real schema<->handler drift that should be fixed, not re-pinned.

# -- declared in a tool schema, no handler read found --
attributeSetPath
boxExtent
cuePath
detectFriendly
effectPath
input
inputB
inputG
luminanceFactors
output
outputB
outputG
outputR
# generate_lods reductionSettings sub-fields: read via the reductionSettings
# object (AssetWorkflowHandlers.cpp ~4134 iterates its Values), not as top-level
# params, so the by-name read check can't see them.
baseLODModel
hardAngleThreshold
maxDeviation
percentTriangles
percentVertices
pixelError
recalculateNormals
weldingThreshold

# -- read from the request payload, not declared in any tool schema --
args
class_path
commands
function
includeSoftDependencies
params
payloadBase64
player_index
subAction
subsystem
# central discriminated-union field: ReadDiscriminatedValue probes every union
# field (incl. vector4Value) to detect which one is set; no action's schema
# advertises FVector4 setting yet, so it reads but isn't declared. Declare it in
# the general-setter fragments if FVector4 props ever need to be settable.
vector4Value
widget_path
z_order
