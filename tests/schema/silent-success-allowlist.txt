# Justified NON-conversions for tests/schema/silent-success-lint.ps1 (one Handle-name per line; # comments).
#
# An entry means: this handler has >=2 Payload->HasField guards (so the lint flags it as a
# bag-setter) but it should NOT get the fail-in-place applied/failed treatment, because it is
# actually one of:
#   - a read-op that merely branches on optional filters (no writes to report), or
#   - a genuinely atomic setter whose missing required value already fails loud, or
#   - a no-op scaffold tracked separately (see fork TODO.md:256-283).
# This is NOT a place to defer real conversion work — pinning an unconverted real bag-setter
# defeats the forcing function. Every entry needs the one-line reason after the '#'.
#
# (populated as the lint surfaces heuristic false positives during the rollout)

HandleSetObjectProperty  # atomic single-property setter: one required propertyName + one discriminated value, all failure paths already loud (INVALID_VALUE/AMBIGUOUS_VALUE/PROPERTY_NOT_FOUND/PROPERTY_CONVERSION_FAILED); the optional reads are target alternatives (objectPath|blueprintPath) + markDirty + value-type discriminants, not a writable bag
HandleAiConfigureSightConfig  # creator, not a bag setter: implements add_sight_config — unconditionally NewObject<UAISenseConfig_Sight> + ConfigureSense every call; an empty request legitimately adds a default sense (sibling DamageSenseConfig is the same pattern, unflagged)
HandleAiConfigureHearingConfig  # creator, not a bag setter: implements add_hearing_config — unconditionally NewObject<UAISenseConfig_Hearing> + ConfigureSense every call; empty request legitimately adds a default sense
HandleAiSetupPerception  # Setup*-that-creates (setup_perception alias of add_perception_component): find-or-creates the perception component and adds default-configured senses; sightRadius/hearingRange etc. are creation defaults, not writes to an existing bag
HandleGasSetModifierMagnitude  # atomic single-value setter: writes exactly one property (Modifiers[i].ModifierMagnitude) in one of two mutually-exclusive representations (value -> ScalableFloat | setByCallerTag -> SetByCaller); modifierIndex/magnitudeType are selectors; validations are all-or-nothing on that single write and already fail loud
HandleEffectSetNiagaraParameter  # atomic single-parameter setter: one required parameterName + one type-discriminated value (floatValue|vectorValue|colorValue|boolValue are mutually-exclusive alternatives selected by parameterType, not a writable bag); already fails loud (SET_NIAGARA_PARAM_FAILED/ACTOR_NOT_FOUND/COMPONENT_NOT_FOUND/INVALID_ARGUMENT)
HandleWidgetAuthoringSetAnchor  # atomic composite setter; optional fields are alternative spellings of one value, seeded from current state, at-least-one guard present
HandleWidgetAuthoringSetAnimationLoop  # NOT_IMPLEMENTED stub, no write path
HandleWidgetAuthoringSetStyle  # combined-branch twin (set_style/set_clipping) dispatched by SubAction; each branch is atomic — set_style is a single-property reflection setter (cf. HandleSetObjectProperty) with loud failure paths, set_clipping a single-value enum setter — not a multi-field bag
HandleWidgetAuthoringSetClipping  # combined-branch twin (set_style/set_clipping) dispatched by SubAction; each branch is atomic — set_style is a single-property reflection setter (cf. HandleSetObjectProperty) with loud failure paths, set_clipping a single-value enum setter — not a multi-field bag
HandleNiagaraSetModuleInput  # atomic single-input setter: one (moduleName, inputName) target per call; floatValue/intValue/boolValue/vectorValue/colorValue/stringValue are a discriminated union for that single input, not a writable bag; no-match and cross-scope ambiguity already fail loud (INPUT_NOT_FOUND/AMBIGUOUS_TARGET)
